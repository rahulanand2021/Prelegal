[project]
name = "prelegal-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "bcrypt>=4.0,<5",
    "fastapi>=0.115",
    "openai>=2.33.0",
    "python-dotenv>=1.2.2",
    "uvicorn[standard]>=0.32",
]

[tool.uv]
package = false
